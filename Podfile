# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'serverForSocketCommunication' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for serverForSocketCommunication

  target 'serverForSocketCommunicationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'serverForSocketCommunicationUITests' do
    # Pods for testing
  end
  pod 'CocoaAsyncSocket' 
end
